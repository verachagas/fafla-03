let letra = prompt("Digite uma letra:");

// Verifica se é maiúscula
let resultado = (letra === letra.toUpperCase()) 
    ? "A letra é MAIÚSCULA" 
    : "A letra é minúscula";

alert(resultado);